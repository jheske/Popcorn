<!--
    See activity_main_two_panes for large-screen version of this activity.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_containers"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/app_bar"
        layout="@layout/app_bar" />

    <Spinner
        android:id="@+id/spinner_sort_by"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:entries="@array/sort_by_array"/>

    <!--
    Layout will be assigned by the fragment's its activity.
    -->
    <FrameLayout
        android:id="@+id/fragment_movie_grid_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
<!--
    <fragment
        android:id="@+id/fragment_movie_grid"
        android:name="com.nano.movies.activities.MovieGridFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:layout="@layout/fragment_movies" />
-->
</LinearLayout>